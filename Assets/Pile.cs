using UnityEngine;

public class Pile : MonoBehaviour
{
    float electromotence;
    bool pointAOccup; //Si le point a un fil qui sort
    bool pointBOccup;
    bool sens;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
